# Guest Next.js App (skeleton)

Start development:

```
cd frontend/guest
npm install
npm run dev
```

The app expects backend API proxied at `/api/v1/*` (configure a proxy in production or use full URL via env variables).
