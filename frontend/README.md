# Frontend

This workspace contains per-role Next.js apps and a `common` package for shared components.

To run the `superadmin` app as an example:

```bash
cd frontend/superadmin
npm install
npm run dev
```
