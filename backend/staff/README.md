# Staff Module - Backend

This folder contains the staff-only backend API for the Hotel Management System.

Quick start

```bash
cd backend/staff
npm install
cp .env.example .env
# edit .env if needed
npm run seed
npm run dev
```

API base: `http://localhost:4001/staff`
